[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
    "dotnet restore TripWiseJa.sln",
    "dotnet publish src/TripWiseJa.API/TripWiseJa.API.csproj -c Release -o publish --self-contained true -r linux-x64"
]

[start]
cmd = "./publish/TripWiseJa.API"
